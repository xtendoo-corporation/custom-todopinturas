<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_payment_selection_list_wizard" model="ir.ui.view">
        <field name="name">pos.payment.selection.list.wizard</field>
        <field name="model">pos.payment</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <list create="0" edit="0" delete="0" class="o_list_table_compact">
                <field name="payment_method_id" string="Método"/>
                <field name="amount" widget="monetary" options="{'currency_field': 'currency_id'}" string="Cantidad"/>
                <field name="currency_id" column_invisible="True"/>
                <button name="action_delete_payment_from_wizard" icon="fa-trash" type="object" class="text-danger" title="Eliminar"/>
            </list>
        </field>
    </record>

    <record id="view_pos_deposit_payment_wizard_payment_line_list_wizard" model="ir.ui.view">
        <field name="name">pos.deposit.payment.wizard.payment.line.list.wizard</field>
        <field name="model">pos.deposit.payment.wizard.payment.line</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <list create="0" edit="0" delete="0" class="o_list_table_compact">
                <field name="payment_method_id" string="Método"/>
                <field name="amount" widget="monetary" options="{'currency_field': 'currency_id'}" string="Cantidad"/>
                <field name="currency_id" column_invisible="True"/>
                <button name="action_delete_payment_from_wizard" icon="fa-trash" type="object" class="text-danger" title="Eliminar"/>
            </list>
        </field>
    </record>

    <record id="view_pos_payment_selection_wizard_form" model="ir.ui.view">
        <field name="name">pos.payment.selection.wizard.form</field>
        <field name="model">pos.payment.selection.wizard</field>
        <field name="arch" type="xml">
            <form string="Seleccione tipo de pago">
                <field name="order_id" invisible="1"/>
                <field name="deposit_wizard_id" invisible="1"/>
                <field name="state" invisible="1"/>
                <field name="document_type" invisible="1"/>
                <field name="partner_id" invisible="1"/>
                <field name="payment_method_ids" invisible="1"/>
                <field name="has_cash_method" invisible="1"/>
                <field name="has_card_method" invisible="1"/>
                <field name="show_deposit_button" invisible="1"/>
                <field name="partner_deposit_enabled" invisible="1"/>
                <field name="partner_deposit_available" invisible="1"/>
                <field name="partner_deposit_warning_message" invisible="1"/>
                <field name="partner_credit_available" invisible="1"/>
                <field name="partner_credit_available_at_open" invisible="1"/>
                <field name="partner_credit_limit_exceeded" invisible="1"/>
                <field name="currency_id" invisible="1"/>

                <div class="container-fluid">
                    <div class="row">
                        <!-- Izquierda: Selecciones -->
                        <div class="col-md-5 border-end">
                            <group string="¿Qué desea hacer?">
                                <field name="operation_type" widget="radio" nolabel="1"
                                       options="{'horizontal': true}"
                                       invisible="deposit_wizard_id"/>
                                <field name="operation_type_deposit" widget="radio" nolabel="1"
                                       options="{'horizontal': true}"
                                       invisible="not deposit_wizard_id"/>
                            </group>

                            <group string="Método de Pago" invisible="operation_type != 'ticket' and operation_type != 'invoice'">
                                <field name="payment_type" widget="radio" nolabel="1"/>
                            </group>

                            <div invisible="partner_id" class="alert alert-warning py-1 mt-3" role="alert">
                                <strong>Aviso:</strong> Para Albarán o Depósito es necesario seleccionar un cliente.
                            </div>
                            <div invisible="not partner_id or partner_deposit_enabled or operation_type != 'deposit'" class="alert alert-warning py-1" role="alert">
                                <strong>Aviso:</strong> El cliente no tiene depósitos habilitados.
                            </div>
                            <div invisible="not partner_id or not partner_deposit_enabled or partner_deposit_available or operation_type != 'deposit'" class="alert alert-warning py-1" role="alert">
                                <field name="partner_deposit_warning_message"/>
                            </div>
                            <div invisible="not partner_id or partner_credit_available_at_open or operation_type != 'delivery'" class="alert alert-warning py-1" role="alert">
                                <strong>Aviso:</strong> El cliente no tiene crédito habilitado en esta caja.
                            </div>
                            <div invisible="not partner_id or not partner_credit_limit_exceeded or operation_type != 'delivery'" class="alert alert-danger py-2" role="alert">
                                <strong>Atención: Límite de crédito superado</strong>
                                <p class="mb-2 text-muted">El cliente ha excedido su límite de riesgo y no se permite confirmar la venta.</p>
                                <div class="row g-1 small">
                                    <div class="col-7 text-start">Límite de riesgo:</div>
                                    <div class="col-5 text-end"><strong><field name="partner_credit_limit"/></strong></div>
                                    <div class="col-7 text-start border-top pt-1 mt-1">Total tras pedido:</div>
                                    <div class="col-5 text-end border-top pt-1 mt-1 text-danger font-weight-bold">
                                        <field name="partner_total_due_after"/>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Derecha: Detalles del Pago -->
                        <div class="col-md-7 ps-4">
                            <group string="RESUMEN DE PAGO" invisible="operation_type != 'ticket' and operation_type != 'invoice'">
                                <div class="w-100 px-2" colspan="2">
                                    <!-- Fila: Total Pedido -->
                                    <div class="row align-items-center mb-3 fw-bold text-uppercase" invisible="amount_paid == 0">
                                        <div class="col-7">Total Pedido:</div>
                                        <div class="col-5 text-end h4 mb-0">
                                            <field name="amount_total" nolabel="1" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
                                        </div>
                                    </div>

                                    <!-- Fila: Pagado -->
                                    <div class="row align-items-center mb-3 fw-bold text-uppercase text-success" invisible="amount_paid == 0">
                                        <div class="col-7">Pagado:</div>
                                        <div class="col-5 text-end h4 mb-0">
                                            <field name="amount_paid" nolabel="1" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
                                        </div>
                                    </div>

                                    <!-- Fila: Total a Pagar -->
                                    <div class="row align-items-center mb-4 fw-bold text-uppercase">
                                        <div class="col-7">Total a Pagar:</div>
                                        <div class="col-5 text-end h4 mb-0">
                                            <field name="amount_due" nolabel="1" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
                                        </div>
                                    </div>

                                    <div invisible="payment_type != 'cash'" class="mt-4 pt-4 border-top">
                                        <!-- Fila: Importe Entregado (Editable) -->
                                        <div class="row align-items-center mb-4 fw-bold text-uppercase">
                                            <div class="col-7">Importe Entregado:</div>
                                            <div class="col-5 text-end d-flex align-items-center justify-content-end h4 mb-0">
                                                <field name="amount_tendered" nolabel="1"
                                                       widget="float"
                                                       options="{'digits': [16, 2]}"
                                                       class="fw-bold text-end h4 mb-0"
                                                       style="width: 120px; border: none; border-bottom: 2px solid #dee2e6; background: transparent; outline: none; padding-right: 5px;"/>
                                                <span class="fw-bold ms-1">€</span>
                                            </div>
                                        </div>

                                        <!-- Fila: Cambio -->
                                        <div class="row align-items-center fw-bold text-uppercase" invisible="amount_change == 0">
                                            <div class="col-7">Cambio / Restante:</div>
                                            <div class="col-5 text-end">
                                                <field name="amount_change" nolabel="1" invisible="amount_change &lt;= 0" widget="monetary" options="{'currency_field': 'currency_id'}" style="color: red; font-size: 1.6rem;"/>
                                                <field name="amount_change_abs" nolabel="1" invisible="amount_change > 0" widget="monetary" options="{'currency_field': 'currency_id'}" style="color: green; font-size: 1.6rem;"/>
                                            </div>
                                        </div>
                                    </div>

                                    <div invisible="payment_type != 'combined'">
                                        <div class="mt-3">
                                            <div class="o_payment_list mb-3" style="max-height: 200px; overflow-y: auto;">
                                                <field name="payment_line_ids" nolabel="1" invisible="deposit_wizard_id"
                                                       context="{'tree_view_ref': 'todopintura_extend_pos_conventional_payment.view_pos_payment_selection_list_wizard'}"/>
                                                <field name="deposit_payment_line_ids" nolabel="1" invisible="not deposit_wizard_id"
                                                       context="{'tree_view_ref': 'todopintura_extend_pos_conventional_payment.view_pos_deposit_payment_wizard_payment_line_list_wizard'}"/>
                                            </div>

                                            <div class="p-3 border rounded bg-white shadow-sm">
                                                <div class="row align-items-center g-3">
                                                    <div class="col-6">
                                                        <field name="selected_payment_method_id" options="{'no_create': True, 'no_open': True}"
                                                               placeholder="Método..." class="w-100 h4 mb-0"/>
                                                    </div>
                                                    <div class="col-4 d-flex align-items-center justify-content-end border-bottom">
                                                        <field name="payment_amount" nolabel="1"
                                                               widget="float"
                                                               options="{'digits': [16, 2]}"
                                                               class="fw-bold text-end h4 mb-0 w-100"
                                                               style="border: none; background: transparent; outline: none;"/>
                                                        <span class="fw-bold ms-1 h4 mb-0">€</span>
                                                    </div>
                                                    <div class="col-2">
                                                        <button name="action_add_payment" string="+" type="object" class="btn btn-primary w-100 h4 mb-0 py-2"/>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </group>

                            <div invisible="operation_type != 'delivery'" class="text-center py-5">
                                <i class="fa fa-truck fa-5x text-muted mb-3"/>
                                <h4>Confirma el pedido como albarán entregado (Crédito).</h4>
                                <p class="text-muted">Se generará la salida de stock y se registrará la deuda en la cuenta del cliente.</p>
                            </div>

                            <div invisible="operation_type != 'deposit'" class="text-center py-5">
                                <i class="fa fa-university fa-5x text-muted mb-3"/>
                                <h4>Registra un depósito a cuenta del cliente.</h4>
                                <p class="text-muted">El importe se descontará de su saldo disponible.</p>
                            </div>
                        </div>
                    </div>
                </div>

                <footer>
                    <button name="action_confirm" string="Confirmar" type="object" class="btn-primary"
                            invisible="(not deposit_wizard_id and operation_type == 'delivery' and (not partner_id or not partner_credit_available_at_open or partner_credit_limit_exceeded)) or (operation_type == 'deposit' and (not partner_id or not partner_deposit_enabled or not partner_deposit_available)) or (deposit_wizard_id and operation_type != 'ticket' and operation_type != 'invoice')"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_pos_payment_selection_wizard" model="ir.actions.act_window">
        <field name="name">Selección de Pago</field>
        <field name="res_model">pos.payment.selection.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
